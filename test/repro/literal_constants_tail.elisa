const LIMIT: i64 = 10

def next(x: i64) -> i64:
    requires x >= 0
    requires x < LIMIT
    ensure result > x
    x + 1

def relay(x: i64) -> i64:
    requires x >= 0
    requires x < LIMIT
    ensure result > x
    next(x)
