extend ElisaProof:
    private:
        # Derived lookup row for a source-level overloaded operator implementation. An empty
        # protocol represents an extension, which shadows every builtin protocol for its type.
        struct ProofOperatorImpl:
            concrete: sview
            protocol: sview
