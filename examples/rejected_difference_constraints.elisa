# The difference solver must reject a relation opposite to the declared ordering.

def wrong_difference(x: i64, y: i64, z: i64) -> void:
    requires x <= y
    requires y < z
    assert x > z by:
        assert x > z
