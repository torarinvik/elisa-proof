# An unresolved/extern call has no verified result summary. The kernel must not let its symbolic
# syntax establish an unrelated postcondition.

extern nondeterministic_value(x: i64) -> i64

def rejected_unknown_call_result(x: i64) -> i64:
    ensure result == 0
    return nondeterministic_value(x)
