# A source binding is unavailable after move. The proof kernel must not preserve the pre-move
# symbolic value into the return obligation.

def rejected_move_use_after(x: i64) -> i64:
    ensure result == 0
    y: i64 = move x
    return x
