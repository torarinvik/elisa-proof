# Compiler libraries use `__elisa_` names for implementation details. Only exact names emitted
# as proof witnesses or fresh proof-state symbols are reserved by the checker.
def internal_prefix_name_is_ordinary(seed: i64) -> i64:
    ensure result == seed
    __elisa_region_cache: i64 = seed
    return __elisa_region_cache
