# Certified pure calls may occur below an otherwise pure runtime expression.
def pure_increment(x: i64) -> i64:
    ensure result == x + 1
    return x + 1

def nested_pure_expression(x: i64) -> i64:
    ensure result == pure_increment(x) + 1
    return pure_increment(x) + 1
