# A checked `get` access is memory-safe even when the index is not statically bounded.
# The fallback is selected by the compiler when the index is outside the collection.
def checked_index_fallback(xs: darray[i64]&, i: i64) -> i64:
    ensure true
    return get xs[i] else 0
