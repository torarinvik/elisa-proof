# Mutable global state cannot be hidden behind a logical pure function summary.

global mutable ambient_value: i64 = 0

def reads_mutable_global() -> i64:
    ensure result == ambient_value
    return ambient_value

def rejected_mutable_global_pure_contract() -> i64:
    ensure result == reads_mutable_global()
    return reads_mutable_global()
