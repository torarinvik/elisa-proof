# A loop transfer after violating the invariant must remain rejected.

def rejected_break_invariant(n: i64) -> i64:
    requires n >= 0
    ensure result >= 0
    x: i64 = 0
    while x < n:
        invariant x >= 0
        x <- x - 1
        break
    return x
