# An invariant must be established at entry and preserved by a symbolic iteration.

def broken_invariant(x: i64) -> void:
    requires x >= 0
    value: mutable i64 = x
    while value >= 0:
        invariant value >= 0
        value <- -1
