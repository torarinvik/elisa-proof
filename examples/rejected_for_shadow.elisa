# A loop binder may shadow a function parameter. The postcondition refers to the parameter,
# not to the loop-local value, so the checker must not substitute the inner name into `ensure`.

def rejected_for_shadow(x: i64) -> i64:
    ensure result == x
    for x in [1]:
        return x
    return x
