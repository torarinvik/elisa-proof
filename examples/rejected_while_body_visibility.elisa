# A loop without an invariant remains rejected, but its body must still be traversed so an
# unsafe access cannot disappear behind the missing-invariant diagnostic.

def rejected_while_body_visibility(values: darray[i64]&, index: usize) -> i64:
    while index < values.count:
        return values[index]
    return 0
