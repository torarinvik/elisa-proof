# A module-local constant must not be substituted with an unrelated top-level constant.
module OtherConstants:
    const LIMIT: i64 = 20

    def wrong() -> i64:
        ensure result == 10
        LIMIT

const LIMIT: i64 = 10
