# Exhaustive bounded model checking proves a nonlinear fixed-width property that interval
# arithmetic alone cannot establish without losing the correlation between the two operands.

def bounded_square_nonnegative(x: i64) -> i64:
    requires x >= -2 and x <= 2
    ensure result >= 0
    return x * x
