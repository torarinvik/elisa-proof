# A closed, pure value match is lowered to the proof kernel's conditional fragment.
def classify_value(x: i64) -> i64:
    ensure result >= 0
    return match x:
        0:
            1
        _:
            0
