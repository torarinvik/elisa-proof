# Exclusive slice endpoints are checked as a lower bound, an upper bound, and an ordering.

def safe_slice(values: darray[i64]&, start: i64, end: i64) -> void:
    requires start >= 0 and start <= end and end <= values.count
    window: view[i64] = values[start:end]
