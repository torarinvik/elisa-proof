# A control-recovery get makes the guarded load safe without requiring the caller to prove
# bounds: an out-of-range index takes the terminating recovery path before loading an element.
def getelse_checked_index(xs: darray[i64], index: i64) -> i64:
    ensure true
    return get xs[index] else return 0
