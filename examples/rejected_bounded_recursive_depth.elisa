# The same ranking without the lower/upper entry facts must remain rejected.
def rejected_bounded_depth(depth: i64) -> bool:
    decreases 127 - depth
    if depth >= 127:
        return false
    return rejected_bounded_depth(depth + 1)

