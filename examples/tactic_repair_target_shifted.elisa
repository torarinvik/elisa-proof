# This unrelated declaration shifts the target's source line and global goal ID without changing
# its canonical proposition or hypotheses.
def unrelated_repair_value() -> i64:
    return 7

def repair_compound_equality(x: i64) -> void:
    requires x + 1 == 3
    ensure (x + 1) * 2 == 6
    return
