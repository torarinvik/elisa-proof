# A branch mutation must invalidate facts carried from before the join point.

def stale_branch_fact(x: i64) -> i64:
    ensure result == 0
    value: mutable i64 = x
    assert value == 0
    if x == 0:
        value <- 1
    return value
