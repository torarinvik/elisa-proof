# Break exits a loop without proving the loop condition is false. The invariant is preserved, but
# the postcondition incorrectly assumes the condition became false, so this remains rejected.

def bad_break(n: i64) -> i64:
    requires n >= 0
    ensure result == 0
    while n > 0:
        invariant n >= 0
        decreases n
        break
    return n
