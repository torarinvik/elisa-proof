# A move in a conditional branch is safe when the moved root is not used afterward.
def resource_branch_join(x: i64) -> i64:
    if x > 0:
        moved: i64 = move x
    else:
        value: i64 = 0
    return 0
