# Structural termination for recursive enum data: `prev` is a proper subterm of `n`.

enum Nat:
    Zero
    Succ(prev: Nat)

def structural_size(n: Nat) -> i64:
    decreases n
    match n:
        Nat.Zero:
            return 0
        Nat.Succ(prev):
            return structural_size(prev)
