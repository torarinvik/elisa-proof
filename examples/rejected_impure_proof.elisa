# Erased proof assertions may not evaluate executable calls, even when the proposition
# is already present as a caller fact.

def identity_value(x: i64) -> i64:
    return x

def impure_proof(x: i64) -> void:
    requires identity_value(x) == x
    assert x == x by:
        assert identity_value(x) == x
