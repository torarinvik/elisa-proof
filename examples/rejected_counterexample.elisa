# The postcondition is false for a concrete input. The proof report should include an exact
# assignment satisfying the empty precondition and falsifying the substituted ensure.

def rejected_counterexample(x: i64) -> i64:
    ensure result < 0
    return x
