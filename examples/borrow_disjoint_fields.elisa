# Named field places are tracked precisely: a borrow of left does not block right.

struct Pair:
    left: mutable i64
    right: mutable i64

def borrow_disjoint_fields(pair: mutable Pair) -> i64:
    left_view: i64& = &pair.left
    pair.right <- 2
    return pair.right
