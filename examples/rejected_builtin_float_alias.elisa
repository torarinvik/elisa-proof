# A source alias that reuses a primitive spelling must not bypass the proof checker's
# floating-point exclusion when alias declarations are inspected.
type i64 = f64

def rejected_builtin_float_alias_reflexivity(value: i64) -> bool:
    ensure value == value
    return true
