# A control-flow get recovery has two independently checked paths: the guarded value path and
# the terminating recovery path. The success path still needs ordinary bounds for its access.
def getelse_return(xs: darray[i64]) -> i64:
    requires 0 < xs.count
    ensure true
    return get xs[0] else return 0
