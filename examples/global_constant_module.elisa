# A literal constant in the function's module is visible to that function and may be imported
# as a proof fact without accidentally importing a same-named constant from another namespace.
module LocalConstants:
    const LIMIT: i64 = 20

    def uses_local_limit() -> i64:
        ensure result == 20
        LIMIT
