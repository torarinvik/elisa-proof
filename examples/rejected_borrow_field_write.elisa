# A write to the exact field covered by a live borrow is rejected by the resource kernel.

struct Pair:
    left: mutable i64

def rejected_borrow_field_write(pair: mutable Pair) -> i64:
    left_view: i64& = &pair.left
    pair.left <- 2
    return pair.left
