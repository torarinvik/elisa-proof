# A write in one branch must not leave the parent path using the pre-branch symbolic value.

def stale_branch_return(x: i64) -> i64:
    requires x >= 0
    ensure result == x
    value: mutable i64 = x
    if x >= 0:
        value <- -1
    return value
