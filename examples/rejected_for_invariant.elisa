# The entry fact holds, but incrementing total violates this upper-bound invariant.

def rejected_for_invariant(n: i64) -> i64:
    requires n >= 0
    ensure result <= 0
    total: mutable i64 = 0
    for index in 0..<n:
        invariant total <= 0
        total <- total + 1
    return total
