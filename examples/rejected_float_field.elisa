struct Sample:
    value: f64

def field_reflexivity(sample: Sample) -> void:
    ensure sample.value == sample.value
    return

struct Envelope:
    sample: Sample

def nested_field_reflexivity(envelope: Envelope) -> void:
    ensure envelope.sample.value == envelope.sample.value
    return
