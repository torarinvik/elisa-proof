# A bounded numeric ranking should be accepted when the recursive edge is protected
# by a guard that establishes the ranking's non-negativity and strict descent.
def bounded_depth(depth: i64) -> bool:
    requires depth >= 0
    requires depth <= 127
    decreases 127 - depth
    if depth >= 127:
        return false
    return bounded_depth(depth + 1)

