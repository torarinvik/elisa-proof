# OR-pattern payload bindings need a joined projection environment and stay unsupported.

enum Option:
    None
    Some(value: i64)

def bind_or(option: Option) -> i64:
    match option:
        Option.Some(value) | Option.None:
            return value
