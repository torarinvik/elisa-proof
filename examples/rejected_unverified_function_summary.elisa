# A failed callee must not be usable as a verified modular summary.

def bad_claim(x: i64) -> i64:
    ensure result == x + 1
    return x

def trusts_bad_claim(x: i64) -> i64:
    requires x >= 0
    ensure result == x + 1
    return bad_claim(x)
