# An unresolved, source-authored predicate must not mint a compiler-derived type marker.
def __elisa_unsigned_type_bound(value: i64, width: i64) -> bool:
    return true

def forged_unsigned_marker(value: i64) -> i64:
    requires __elisa_unsigned_type_bound(value, 8)
    ensure result >= 0
    return value
