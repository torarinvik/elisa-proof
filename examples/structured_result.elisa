# Structural result substitution covers constructor fields without introducing an SMT axiom.

struct Pair:
    left: i64
    right: i64

def make_pair(x: i64) -> Pair:
    ensure result.left == x
    ensure result.right == 0
    return Pair{left: x, right: 0}
