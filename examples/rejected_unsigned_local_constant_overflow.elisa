# A typed local must not turn a wrapping initializer into an exact signed fact.
def rejected_unsigned_local_constant_overflow() -> u8:
    ensure result == 256
    value: u8 = 255 + 1
    return value

def main() -> u8:
    return rejected_unsigned_local_constant_overflow()
