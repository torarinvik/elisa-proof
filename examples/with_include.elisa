include "./included.elisa"
include "./empty_include.elisa"

def imported_contract(x: i64) -> i64:
    requires x >= 0
    ensure result == x
    return x

def imported_proof(x: i64) -> void:
    requires x >= 0
    assert x >= 0 by:
        assert x >= 0
