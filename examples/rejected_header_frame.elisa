# Header-form frame metadata must constrain the executable body as well.

struct Box:
    value: mutable i64
    other: mutable i64

def writes_outside_header(box: mutable Box&) changes box.value:
    box.other <- 1
