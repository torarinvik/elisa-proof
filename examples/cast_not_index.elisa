# Type-cast brackets are not runtime indexing and must not create collection-bound goals.
def cast_not_index(x: i64) -> i64:
    return x.cast[i64]

def generic_cast_not_index[T](x: T) -> T:
    return x.cast[T]
