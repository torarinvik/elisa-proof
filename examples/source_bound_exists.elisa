# Source-bound tactic replay must use the compiler's existential annotation, not a script label.

def source_bound_exists() -> i64:
    requires exists i in [0, 1]: i == 1
    ensure 0 == 1
    return 1
