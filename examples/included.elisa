# This file is intentionally separate so the proof front end exercises Elisa's textual include
# convention before it builds the shared compiler AST.
def library_identity(x: i64) -> i64:
    ensure result == x
    return x
