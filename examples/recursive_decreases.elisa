# Direct recursion is summarized only after a checked well-founded step.

def countdown(n: i64) -> i64:
    requires n >= 0
    decreases n
    ensure result == 0
    if n > 0:
        return countdown(n - 1)
    return 0
