# Resource operations are executable effects, not transparent proof expressions. They remain
# rejected until the kernel tracks ownership, allocation, and region lifetime explicitly.

def rejected_move_contract(x: i64) -> i64:
    ensure move x == x
    return x
