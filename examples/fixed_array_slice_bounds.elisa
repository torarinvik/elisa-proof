# Fixed-array slices use the same exact-count relation as single indexes.

def fixed_slice(values: i64[4]&, start: usize, end: usize) -> void:
    requires start <= end and end <= 4
    window: view[i64] = values[start:end]
