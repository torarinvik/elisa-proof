# A borrow created in a child branch expires with that branch and does not block a later write.
def borrow_lexical_scope(x: mutable i64) -> i64:
    if true:
        view: i64& = &x
        _ = view
    x <- 1
    return x
