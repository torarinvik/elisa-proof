# A weaker endpoint theorem must not be widened to the declared fixed extent.

def rejected_fixed_slice(values: i64[4]&, start: usize, end: usize) -> void:
    requires start <= end and end <= 5
    window: view[i64] = values[start:end]
