# Both directives name the same file after lexical cleaning; the compiler and prover must splice
# it once, even though the second path contains a nested `..` alias.
include "./included.elisa"
include "./sub/../included.elisa"
