# The multi-index path is exact, so the same coordinate must still be rejected as an alias.
def rejected_borrow_multi_index_alias(values: mutable array[array[i64, 2], 2]) -> i64:
    shared: i64& = &values[0, 1]
    exclusive: mutable i64& = &values[0, 1]
    return values[0, 1]
