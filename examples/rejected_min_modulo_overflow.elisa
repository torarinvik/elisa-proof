# A bounded remainder proof must not hide the fixed-width MIN_I64 % -1 overflow edge.

def rejected_min_modulo_overflow(x: i64) -> i64:
    requires x == -9223372036854775807 - 1
    ensure result == 0
    return x % -1
