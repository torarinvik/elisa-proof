# Moving a field disjoint from a live borrow is permitted by the place kernel.

struct Pair:
    left: mutable i64
    right: mutable i64

def borrow_move_disjoint_field(pair: mutable Pair) -> i64:
    left_view: i64& = &pair.left
    moved: i64 = move pair.right
    return moved
