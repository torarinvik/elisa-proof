# A move on one branch makes the root unavailable after the branch join.
def rejected_resource_branch_move(x: i64) -> i64:
    if x > 0:
        moved: i64 = move x
    else:
        value: i64 = 0
    return x
