# Source-bound branch fixture: the imported goal must be the proposition that the nested script
# actually closes; the script cannot replace this state with its own initial JSON proposition.
def prove_branch() -> void:
    proof (true and true) and true:
        assert (true and true) and true
