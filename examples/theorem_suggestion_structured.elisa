lemma tuple_fact(value: i64):
    requires (value * 2, value) == (6, 3)
    ensure (value * 2, value) == (6, 3)
    assert (value * 2, value) == (6, 3)

def tuple_suggestion_target(x: i64) -> i64:
    requires ((x + 1) * 2, x + 1) == (6, 3)
    ensure ((x + 1) * 2, x + 1) == (6, 3)
    return 6
