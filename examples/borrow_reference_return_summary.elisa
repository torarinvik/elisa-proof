# A direct reference-formal return can be composed when the caller passes an external reference.
# The proof system tracks that provenance instead of rejecting every reference-returning call.
def return_reference(value: i64&) -> i64&:
    return value

def borrow_reference_return_summary(value: i64&) -> i64&:
    return return_reference(value)
