# Exact place tracking must still reject the same literal element when one borrow is mutable.
def rejected_borrow_index_alias(values: mutable i64[1]) -> i64:
    shared: i64& = &values[0]
    exclusive: mutable i64& = &values[0]
    return values[0]
