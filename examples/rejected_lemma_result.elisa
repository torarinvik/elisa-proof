# A result-dependent lemma cannot be invoked as a bare proof statement.

lemma returning_fact() -> i64:
    ensure result == result
    return 0

def unbound_result_fact() -> void:
    proof true:
        returning_fact()
