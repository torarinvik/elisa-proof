# A universal quantifier fails closed when one finite instance is false.

def bad_bounded_forall() -> i64:
    ensure forall i in 0 ..< 3: i != 1
    return 0

def too_large_bounded_forall() -> i64:
    ensure forall i in 0 ..< 65537: i >= 0
    return 0
