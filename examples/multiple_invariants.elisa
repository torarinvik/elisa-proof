# All loop invariants are available while checking one symbolic body iteration.

def multiple_invariants(x: i64) -> i64:
    requires x >= 0
    value: mutable i64 = x
    while value > 0:
        invariant value >= 0
        invariant x >= 0
        decreases value
        value <- value - 1
    return value
