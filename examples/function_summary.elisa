# Ordinary executable calls are checked against the callee's verified contract.

def identity_nonnegative(x: i64) -> i64:
    requires x >= 0
    ensure result == x
    return x

def caller_uses_summary(x: i64) -> i64:
    requires x >= 0
    ensure result == x
    return identity_nonnegative(x)
