# The first resource-state slice admits lexical shared borrows and multiple shared aliases.
# No write, move, call, or escape crosses the live borrow.
def borrow_shared_read(x: i64) -> i64:
    first: i64& = &x
    second: i64& = &x
    return x
