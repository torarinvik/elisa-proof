# A recursive call that does not decrease its declared measure is rejected.

def bad_countdown(n: i64) -> i64:
    requires n >= 0
    decreases n
    ensure result == 0
    if n > 0:
        return bad_countdown(n)
    return 0
