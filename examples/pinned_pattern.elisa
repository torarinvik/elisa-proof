# A pin pattern compares the scrutinee with an existing binding.

def pinned_return(x: i64, expected: i64) -> i64:
    requires x == expected
    ensure result == x
    match x:
        ^expected:
            return expected
        _:
            return x
