# A callee's requires clause is an obligation at every executable call site.

def positive_identity(x: i64) -> i64:
    requires x > 0
    ensure result == x
    return x

def caller_lacks_precondition(x: i64) -> i64:
    requires x >= 0
    ensure result == x
    return positive_identity(x)
