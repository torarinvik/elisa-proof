# A valid target certificate must not launder an independently ill-formed source program.

def repair_compound_equality(x: i64) -> void:
    requires x + 1 == 3
    ensure (x + 1) * 2 == 6
    return

def unresolved_name() -> i64:
    return missing_value
