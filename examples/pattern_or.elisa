# Closed OR-patterns contribute one disjunctive fact to their match arm.

def classify_or(x: i64) -> i64:
    ensure result >= 0
    match x:
        0 | 1:
            assert x == 0 or x == 1 by:
                assert x == 0 or x == 1
            return 0
        _:
            return 1
