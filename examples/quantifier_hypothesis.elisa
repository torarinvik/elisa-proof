# A quantified precondition must retain its kind in the certificate context.
def existential_context() -> i64:
    requires exists i in [0, 1]: i == 1
    ensure true
    return 0

def universal_context() -> i64:
    requires forall i in [0, 1]: i >= 0
    ensure true
    return 0
