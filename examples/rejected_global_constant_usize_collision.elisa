# A module-local machine-sized constant must not be replaced by an unrelated root constant.
module LocalUnsignedConstants:
    const LIMIT: usize = 20

    def wrong() -> usize:
        ensure result == 10
        LIMIT

const LIMIT: usize = 10
