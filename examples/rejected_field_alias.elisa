# Distinct field receivers must remain distinct in independent certificate replay.

struct Cell:
    value: i64

def rejected_field_alias(left: Cell, right: Cell) -> i64:
    requires left.value == 0
    ensure result == right.value
    return left.value
