# Moving a parent field overlaps a live borrow of its descendant.

struct Inner:
    value: mutable i64

struct Box:
    inner: mutable Inner

def rejected_borrow_move_parent(box: mutable Box) -> i64:
    value_view: i64& = &box.inner.value
    moved: Inner = move box.inner
    return 1
