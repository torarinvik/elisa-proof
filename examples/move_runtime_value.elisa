# Runtime moves are state transitions. The proof kernel may carry the moved value out, but it
# cannot reuse the source binding or preserve facts across the transfer.

def move_runtime_value(x: i64) -> i64:
    ensure result == result
    y: i64 = move x
    return y
