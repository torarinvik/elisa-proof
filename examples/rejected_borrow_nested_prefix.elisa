# A borrow of a parent field overlaps writes to every descendant of that field.

struct Inner:
    value: mutable i64

struct Box:
    inner: mutable Inner

def rejected_borrow_nested_prefix(box: mutable Box) -> i64:
    inner_view: Inner& = &box.inner
    box.inner.value <- 1
    return box.inner.value
