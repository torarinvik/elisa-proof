# `0 - x <= 0` means `x >= 0`, so an upper bound alone must not prove it.
def rejected_negative_affine_goal(x: i64) -> i64:
    requires x >= -10
    requires x <= 0
    ensure 0 - x <= 0
    return x

def main() -> i64:
    return rejected_negative_affine_goal(-1)
