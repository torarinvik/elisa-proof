# Each failed callee precondition is a distinct repairable goal, even when both arise at one call.

def bounded_value(x: i64) -> i64:
    requires x >= 0
    requires x < 5
    return x

def unchecked_bounded_value(x: i64) -> i64:
    return bounded_value(x)
