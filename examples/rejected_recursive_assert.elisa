# Runtime assertions are executable: a recursive call hidden in one cannot bypass SCC checks.

def circular_runtime_assert(n: i64) -> bool:
    requires n >= 0
    ensure result == true
    assert circular_runtime_assert(n)
    return true
