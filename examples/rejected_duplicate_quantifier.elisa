# Dictionary quantifier binders are distinct variables. Reusing one name would
# make sequential substitution ambiguous and must not enter the proof kernel.

def duplicate_dictionary_binder() -> i64:
    ensure forall (x, x) in {1: 1}: true
    return 0

def main() -> i64:
    return duplicate_dictionary_binder()
