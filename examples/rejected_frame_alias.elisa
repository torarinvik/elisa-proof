# A local reference alias must not bypass a preserved whole-root frame.

struct Box:
    value: mutable i64
    other: mutable i64

def writes_through_alias(box: mutable Box&) -> void:
    preserves box
    alias: mutable Box& = box
    alias <- box
