# A value-returning call is not a proposition. The source adapter and compiler semantic
# boundary must reject it even though the source-neutral term arena can represent the call.
def integer_value() -> i64:
    return 1

def rejected_nonbool_assert_call() -> void:
    assert integer_value()
