# A for binder is not in scope after the loop and cannot be exported by an invariant.

def rejected_for_invariant_scope(n: i64) -> i64:
    requires n >= 0
    ensure result == 0
    total: mutable i64 = 0
    for index in 0..<n:
        invariant index >= 0
        total <- total + 1
    return 0
