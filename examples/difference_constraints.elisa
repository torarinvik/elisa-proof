# Difference-constraint closure proves transitive strict and non-strict comparisons.

def transitive_difference(x: i64, y: i64, z: i64) -> void:
    requires x <= y
    requires y < z
    assert x <= y by:
        assert x <= y
    assert x < z by:
        assert x < z
