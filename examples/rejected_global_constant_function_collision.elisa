# The report model currently records an owner function by bare name. Two module-local functions
# with that name are therefore ambiguous at the replay boundary and must fail closed.
module First:
    const LIMIT: i64 = 10
    def check() -> i64:
        ensure result == 10
        LIMIT

module Second:
    def check() -> i64:
        ensure result == 10
        return 10
