# A parallel proof-state barrier must propagate through enclosing sequential joins.

def nested_parallel_fact_barrier(values: darray[i64], total: i64) -> i64 can[Pool.Submit, Pool.WaitAll]:
    requires total == 0
    ensure result == 0
    if true:
        parallel for value in values:
            pass
    return total
