# A recovery block that falls through is not yet a verified get/try control-flow form. It must
# not be treated as if the guarded value were unconditionally available.
def rejected_getelse_recovery(xs: darray[i64]) -> i64:
    ensure true
    return get xs[0] else failure:
        marker: i64 = 0
