# Header-form frame clauses are imported from the compiler metadata side table.

struct Box:
    value: mutable i64
    other: mutable i64

def set_header(box: mutable Box&) changes box.value:
    box.value <- 1
