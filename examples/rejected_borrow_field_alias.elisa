# A mutable borrow of a field conflicts with an earlier shared borrow of that same field.

struct Pair:
    left: mutable i64

def rejected_borrow_field_alias(pair: mutable Pair) -> i64:
    shared: i64& = &pair.left
    exclusive: mutable i64& = &pair.left
    return pair.left
