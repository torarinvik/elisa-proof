# A mutable direct borrow owns the only write capability for its root in this subset.
def borrow_mutable_write(x: mutable i64) -> i64:
    exclusive: mutable i64& = &x
    exclusive <- 3
    return x
