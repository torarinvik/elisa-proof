# The outer checked access is safe, but an unchecked access in its index expression remains
# a real memory-safety obligation and must not be hidden by the outer fallback.
def rejected_checked_index_nested(xs: darray[i64]&, indexes: darray[i64]&, i: i64) -> i64:
    ensure true
    return get xs[indexes[i]] else 0
