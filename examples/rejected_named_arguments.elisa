# A reordered named argument must still satisfy the formal precondition.

def requires_first(first: i64, second: i64) -> i64:
    requires first >= 0
    return first

def bad_named_caller(x: i64) -> i64:
    return requires_first(second: 7, first: x)
