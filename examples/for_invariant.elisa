# Ordinary iterable loops can export a checked invariant across their iteration join.

def count_nonnegative(n: i64) -> i64:
    requires n >= 0
    ensure result >= 0
    total: mutable i64 = 0
    for index in 0..<n:
        invariant total >= 0
        total <- total + 1
    return total
