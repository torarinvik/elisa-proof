# Passing the original enum value back is not a structural decrease.

enum Nat:
    Zero
    Succ(prev: Nat)

def bad_structural_size(n: Nat) -> i64:
    decreases n
    match n:
        Nat.Zero:
            return 0
        Nat.Succ(prev):
            return bad_structural_size(n)
